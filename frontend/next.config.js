// Arquivo de configuração do Next.js
// next.config.js

/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['www.tibiawiki.com.br'],
  },
}

module.exports = nextConfig

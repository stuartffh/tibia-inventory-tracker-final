{
  "name": "tibia-inventory-tracker",
  "version": "1.0.0",
  "description": "Sistema de gerenciamento de inventário para Tibia",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "frontend:build": "cd frontend && npm run build",
    "frontend:start": "cd frontend && npm run start",
    "install-all": "npm install && cd frontend && npm install",
    "build-all": "npm run frontend:build",
    "start-all": "concurrently \"npm run start\" \"npm run frontend:start\""
  },
  "dependencies": {
    "bcrypt": "^5.1.0",
    "better-sqlite3": "^8.3.0",
    "concurrently": "^8.0.1",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
